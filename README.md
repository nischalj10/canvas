# canvas - a prototype of node based ui (like miro boards)

Built with an awesome library [reactflow](https://reactflow.dev/)


## Getting up and running

```bash
npm install # or `pnpm install` or `yarn install`
```

```bash
npm run dev
```
